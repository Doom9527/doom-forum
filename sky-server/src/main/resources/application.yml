server:
  port: 8999

spring:
  profiles:
    active: dev
  main:
    allow-circular-references: true
  datasource:
    druid:
      driver-class-name: ${sky.datasource.driver-class-name}
      url: jdbc:mysql://${sky.datasource.host}:${sky.datasource.port}/${sky.datasource.database}?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&useSSL=false&allowPublicKeyRetrieval=true
      username: ${sky.datasource.username}
      password: ${sky.datasource.password}

  redis:
    host: 111.230.69.151
    port: 6379
    password: 123456789
    lettuce:
      pool:
        max-active: 8
        max-idle: 8
        min-idle: 0
        max-wait: 100ms

  rabbitmq:
    host: 111.230.69.151 # 你的虚拟机IP
    port: 5672 # 端口
    virtual-host: /javaCoreHomework # 虚拟主机
    username: redo # 用户名
    password: 123 # 密码
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

mybatis:
  #mapper配置文件
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.sky.entity
  configuration:
    #开启驼峰命名
    map-underscore-to-camel-case: true

mybatis-plus:
  global-config:
    db-config:
      id-type: auto


logging:
  level:
    com:
      sky:
        mapper: debug
        service: info
        controller: info


#sky:
#  jwt:
#    # 设置jwt签名加密时使用的秘钥
#    admin-secret-key: itcast
#    # 设置jwt过期时间
#    admin-ttl: 7200000
#    # 设置前端传递过来的令牌名称
#    admin-token-name: token
swagger:
  enabled: true

# 集成阿里云OSS
aliyun:
  oss:
    file:
      endpoint: oss-cn-shenzhen.aliyuncs.com
      keyid: LTAI5tJcoYMsoFba4oLFCt9d
      keysecret: hxRVKqn8gc8sCBHkbkeH2HgS2HHreB
      bucketname: redo-mall